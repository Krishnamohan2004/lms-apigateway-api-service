apiVersion: v1
kind: Service

metadata:
  name: apigateway
  namespace: lms

spec:

  type: LoadBalancer

  selector:
    app: apigateway

  ports:

    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
